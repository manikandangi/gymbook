name: Android Build

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ✅ Checkout
      - name: Checkout repo
        uses: actions/checkout@v4

      # ✅ Setup Node (with cache)
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      # ✅ Install dependencies (fast)
      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --progress=false

      # ✅ Setup Java (required)
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "17"

      # ✅ Setup Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      # ✅ Cache Expo prebuild (android/ios folders)
      - name: Cache Expo Prebuild
        uses: actions/cache@v4
        with:
          path: |
            android
            ios
          key: ${{ runner.os }}-expo-${{ hashFiles('package.json') }}
          restore-keys: |
            ${{ runner.os }}-expo-

      # ✅ Cache Gradle (huge speed boost)
      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('android/**/*.gradle*') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      # ✅ Create android folder first
      - name: Expo Prebuild
        run: npx expo prebuild --platform android

      # ✅ Increase Gradle memory (after folder exists)
      - name: Increase Gradle memory
        run: echo "org.gradle.jvmargs=-Xmx4g" >> android/gradle.properties

      # ✅ Build APK
      - name: Build APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease \
            --no-daemon \
            --parallel \
            --build-cache \
            -x lint \
            -x test

      # ✅ Upload APK
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/app-release.apk